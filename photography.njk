---
layout: base.njk
title: Work
description: A portfolio of sorts.
subtitle: Case studies…someday?
redirect_from: "/photos"
---

{# <ul reversed class="work-archive">
{% for post in collections.work | reverse %}
  <li>
    <img src="{{ post.data.thumb }}" alt="">
    <a href="{{ post.url | url }}" class="postlist-link">{{ post.data.title }}</a>
    <time datetime="{{ post.date.toUTCString() }}">{{ post.date | readableYear}}</time>
  </li>
{% endfor %}
</ul> #}

<div class="wrap">
  <h1 class="page-title">Photography</h1>
</div>

<section class="workboard photography">
  {% for piece in photos %}
  <div class="workboard-item">
    {% if piece.link %}<a href="{{ piece.link }}">{% endif %}
      <img src="{{ piece.img }}" alt="{{piece.alt}}" {% if piece.title %}title="{{piece.title}}"{%endif%}>
    {% if piece.link %}</a>{% endif %}
    </div>
  {% endfor %}
</section>

{# <img src="/src/img/wety.svg" style="width: 100%;" alt="works end, thank you"> #}