---
title: My Favourite Recipes
layout: page.njk
date: 2026-02-23 10:27:00 -06:00
updated: true
---

<ul class="feature-list">
    {% for recipe in recipes %}
    
        <li>
            <a href="{{ recipe.url }}">
                {# {% if recipe.img %}<img src="{{ recipe.img }}" alt="" loading="lazy">{% endif %} #}
                {# {% if recipe.img %}<img src="{{ recipe.img }}" alt="" loading="lazy" style="height: 3em; width: 3em; object-fit: cover; display: inline-block; margin-inline-end: 1ch; margin-block: 0; margin-block-end: -1em; border-radius: 0.5em;">{% endif %} #}
            <strong>{{ recipe.title }}</strong>
            </a> 
            {% if recipe.author %}&nbsp;by {{ recipe.author }} {% endif %} 
            {% if recipe.publisher %} for {{ recipe.publisher }}{% endif %}
            {% if recipe.book %} from <em>{{ recipe.book }}</em>{% endif %}
        </li>
    {% endfor %}
</ul>