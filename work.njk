---
layout: base.njk
title: Work
description: A portfolio of sorts.
subtitle: Case studies…someday?
---

{# <ul reversed class="work-archive">
{% for post in collections.work | reverse %}
  <li>
    <img src="{{ post.data.thumb }}" alt="">
    <a href="{{ post.url | url }}" class="postlist-link">{{ post.data.title }}</a>
    <time datetime="{{ post.date.toUTCString() }}">{{ post.date | readableYear}}</time>
  </li>
{% endfor %}
</ul> #}

<div class="wrap">
  <h1 class="page-title">Work</h1>
  <p>More, someday. Contact <a href="https://collectif.co">Collectif</a> to make some more.</p>
</div>

<section class="workboard">
  {% for piece in work %}
    <figure class="workboard-item">
    {% if piece.link %}<a href="{{ piece.link }}">{% endif %}
      <img src="{{ piece.img }}" alt="{{piece.alt}}" loading="lazy">
    {% if piece.link %}</a>{% endif %}
    {%if piece.title%}
      <figcaption class="work-caption">{{piece.title}}{%- if piece.title and piece.year -%}, {%endif%}{%- if piece.year -%}{{ piece.year }}{%- endif -%}</figcaption>
    {%endif%}
    </figure>
  {% endfor %}
</section>

<div class="wrap">
  <span>Works end. Thank you.</span>
</div>